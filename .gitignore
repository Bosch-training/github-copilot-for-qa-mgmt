# Secrets / local-only credentials — never commit
.env
.env.*
!.env.example
**/*.local.md
**/*.local.env
**/*.local.txt

# OS / editor cruft
.DS_Store
Thumbs.db

# PowerPoint presentation files (source decks — PDFs in presentation/ are the tracked deliverables)
*.pptx
*.potx
